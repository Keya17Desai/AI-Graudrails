global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "chatbot_api"
    static_configs:
      # host.docker.internal works on Mac/Windows; on Linux use the docker bridge gateway IP
      - targets: ["172.17.0.1:8000"]
    metrics_path: "/metrics"
